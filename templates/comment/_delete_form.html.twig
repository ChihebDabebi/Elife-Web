<form method="post" action="{{ path('app_comment_delete', {'id': comment.idRep}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.idRep) }}">
    <button class="btn">Delete</button>
</form>