{% extends 'base.html.twig' %}

{% block title %}Modifier l'Espace{% endblock %}

{% block body %}
     <main id="main" class="main">
      <div class="container">
        <h1 class="mt-4 mb-4"> Modifier l' Espace</h1>

        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card">
                    <div class="card-body">
        {{ include('espace/form.html.twig') }}
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('app_espace') }}" class="btn btn-secondary">Retour à la liste</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

            </main><!-- End #main -->

{% endblock %}