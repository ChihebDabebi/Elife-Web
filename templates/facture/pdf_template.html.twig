<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Facture</title>
<style>
   body {
    font-family: 'Verdana', sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f7f7f7;
  }
  .invoice-box {
    width: 595px;
    height: 842px;
    margin: auto;
    padding: 30px;
    border: 1px solid #ccc;
    box-shadow: 0 0 10px rgba(0, 0, 0, .15);
    background-color: #fff;
  }

  .header {
    text-align: center;
    margin-bottom: 20px;
  }
  .details {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
  }
  .details th, .details td {
    padding: 10px;
    border-bottom: 1px solid #eee;
  }
  .details th {
    background-color: #f5f5f5;
  }
  .total {
    margin-top: 20px;
    text-align: right;
    font-weight: bold;
  }
  .footer {
    margin-top: 30px;
    text-align: center;
    color: #888;
  }
  .logo {
    text-align: center;
    margin-bottom: 20px;
  }
  .logo img {
    max-width: 150px;
    height: auto;
  }
</style>
</head>
<body>
<div class="invoice-box">
  <div class="logo">" 
    <img src="{{ asset('public/logo.jpg')  }}" >
  </div>
  <div class="header">
    <h1>Facture</h1>
   
  </div>
  <table class="details">
    <thead>
      <tr>
        <th>Numéro de facture</th>
        <th>Type de facture</th>
        <th>Date de facturation</th>
        <th>Montant</th>
        <th>Consommation</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>{{ facture.numFacture }}</td>
        <td>{{ facture.type }}</td>
        <td>{{ facture.date ? facture.date|date('d/m/Y') : '' }}</td>
        <td>{{ facture.montant }}</td>
        <td>{{ facture.consommation }}</td>
      </tr>
    </tbody>
  </table>

  <div class="footer">
    <p>Conditions de paiement : Net 30 jours. Merci de votre confiance.</p>
    <p>Contact : WirelessSolution@Elife.com | Tél : +21695374008</p>
  </div>
</div>
</body>
</html>
